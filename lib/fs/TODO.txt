Per tutti i file, prendere con extern le strutture e le variabili importanti
come la grandezza del disco.

Creare un file contentente le funzioni CRUD per il disco
quindi scrivi settore, cancella settore, modifica settore eccetera.
	
	All'interno del file per CRUD le funzioni devono essere strutturate
	nel seguente modo
	
	es:
		void scrivi_settore (unsigned long int numero_settori, unsigned long int partenza, char (ci va una struttura apposta perche il singolo carattere del settore è  byte)buffer){
			if (!lba48){

			}else{
			
			}
		}

Creare un file per fare la deframentazione del disco, quindi rendere 
contigui i settori di un file.

Creare un file (generale) che sia il core del filesystem, quindi dove vengono
getite tutte le richieste per il disco.

Fare due filesystem 
uno che si struttura con la scrittura di settori in modo sequenziale:
1° settore un file 
2° settore un secondo file 
3° settore continuazione del primo file che ha dopo la firma iniziale il numero del settore a cui fa riferimento (oppure il nome del file) e nel primo settore il numero del terzo settore 

Secondo filesystem che si struttura come il primo, ma permette l'indicizzazione in RAM dei file presenti attraverso un typedef struct __attribute__((packed)) con: nomeFile, indirizzo, tipoFile

In entrambi i filesystem si può fare la deframmentazione

IN IO_SETTORE:

aggiungere le firme per i settori normali (0xbb) e per i settori avviabili (0xaa)


IN LEGGI SETTORE LEGGO UNA WORD DA 16 BIT E LA METTO SUBITO NELL'ARRAY PERCHE ME NE FRGO DELGLI ALTRI BIT

Fose bisogna ridurre la letturura e dei settori da 512 a 256 perchè sono 256 word da 16bit per settore di conseguenza se leggo per 512 interrogo due volte il settore
e vedo la pria volta i valori poi vedo il seguto e poi va a scalare ai settori sotto dandomi sempre 0x00
